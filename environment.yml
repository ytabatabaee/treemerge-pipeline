name: treemerge
channels:
  - conda-forge
  - bioconda

dependencies:
  # ---- Python ----
  - python=3.9

  # ---- Core numeric stack (pinned for ABI stability) ----
  - numpy=1.23.*
  - scipy=1.10.*

  # ---- Tree / phylogenetics ----
  - ete3=3.1.3
  - six
  - dendropy=4.6.1
  - networkx
